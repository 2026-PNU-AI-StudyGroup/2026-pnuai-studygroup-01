# 프로젝트 관리 시스템 설계 및 구현 계획

## 1. 문서 목적

학과에서 수시로 열리는 졸업과제, 교내외 대회, 교육 프로그램의 개설부터 주제 공개, 팀 구성, 진행 관리, 보고서 승인, 결과물 보관까지 하나의 웹 서비스에서 처리한다. 프로그램 이름과 분류는 코드에 고정하지 않고 관리자가 운영 시점에 등록한다.

## 2. 확정된 제품 방향

### 사용자와 인증

- 사용자 역할은 `학생`, `교수`, `관리자`로 구분한다.
- 로그인은 Google OAuth만 제공한다.
- Google 로그인 요청 시 `hd=pusan.ac.kr`를 사용하고, 서버가 반환된 ID Token의 서명, 발급자, 대상, 만료, `email_verified=true`, `hd=pusan.ac.kr`를 검증한다.
- 사용자 식별자는 변경 가능한 이메일이 아니라 Google의 불변 식별자인 `sub`를 사용한다.
- 신규 사용자의 기본 역할은 학생이다.
- 교수 권한은 관리자 승인 또는 사전 등록된 교수 이메일 허용 목록을 통해서만 부여한다. 이메일 도메인만으로 교수 권한을 자동 부여하지 않는다.

### 기술 스택

| 영역 | 선택 | 이유 |
|---|---|---|
| 애플리케이션 | Next.js 16 App Router, TypeScript | 화면과 API를 하나의 배포 단위로 운영하되 서버/클라이언트 경계를 명확히 유지 |
| UI | Tailwind CSS, shadcn/ui | 접근성 있는 공통 컴포넌트와 빠른 화면 구성 |
| 인증 | Better Auth, Google OAuth/OIDC | Next.js 통합과 Google Workspace `hd` 검증 지원 |
| 데이터베이스 | PostgreSQL | 관계형 업무 데이터, 트랜잭션, 전문 검색 지원 |
| ORM | Prisma | 명시적인 스키마, 마이그레이션, 타입 안전성 |
| 입력 검증 | Zod | 화면 폼과 서버 입력 검증 규칙 공유 |
| 파일 저장 | S3 호환 Object Storage | 보고서, 발표 영상, 결과물의 대용량·버전 관리 |
| 로컬 LLM | macOS 네이티브 Ollama + `qwen3.5:2b` | 외부 AI API 없이 M3 GPU를 활용하는 최소 실사용 모델 |
| 테스트 | Vitest, React Testing Library, Playwright | 도메인 단위 테스트부터 실제 사용자 흐름까지 검증 |
| 로컬 인프라 | Docker Compose의 PostgreSQL + MinIO | DB와 Object Storage를 재현 가능한 단일 명령으로 실행 |
| 배포 | Docker 기반 Next.js 서버 + PostgreSQL + Object Storage | 장기 실행 작업과 업로드를 서버리스 제약 없이 운영 |

`모든 기능을 Next.js로 구현한다`는 것은 하나의 코드 저장소와 하나의 웹 애플리케이션을 뜻한다. 도메인 로직을 React 컴포넌트나 Route Handler에 직접 넣는 구조는 사용하지 않는다.

### 최소 요구사항 추적 기준

학과 전체 프로젝트 프로그램으로 범위를 확장하더라도 아래 원 요구사항은 제거하거나 축소하지 않는다. 기능 변경과 최종 검증은 이 표를 상위 완료 기준으로 대조한다.

| 구분 | 원 요구사항 | 제품 기능 및 검증 기준 |
|---|---|---|
| 교수 | 주제 공개 | 열린 프로그램 안에서 주제 작성·공개·마감 |
| 교수 | 학생 지원 조건의 공개·형식화 | 필수·우대 기술, 희망 역할, 활동 가능 시간의 구조화 입력·조회 |
| 교수 | 팀 선택 | 지원자 수락·거절, 정원·동일 학기 단일 팀 제약, 팀 확정 |
| 교수 | 팀별 진행 상황 확인 | 지도 팀 목록, 마일스톤 기반 진행률, 진행 기록과 위험 요소 확인 |
| 교수 | 착수·중간 보고서 확인 및 인증 | 보고서 버전별 웹 승인·수정 요청·재제출 이력 보존. 결과 보고서도 동일 흐름으로 지원 |
| 교수 | 지도 팀과의 소통 | 팀 워크스페이스의 기록형 토론과 조언·응답 |
| 학생 | 주제 탐색 및 지원 | 프로그램별 공개 주제 탐색, 모집 기간·정원 확인, 구조화 지원 |
| 학생 | 팀원 모집 및 토론·영어 지원 | 학생 주도 모집 글·지원·수락과 로컬 LLM 기반 한영 번역 |
| 학생 | 지난 프로젝트 탐색 | 완료 프로젝트 아카이브와 공개 결과물 조회 |
| 학생 | 착수·중간·결과 보고서 제출 | 파일 무결성 검증, 버전 제출, 승인 상태 확인 |
| 학생 | 결과물·발표 영상 관리 | 파일 또는 외부 링크 결과물 등록과 유형별 조회 |
| 실무 관리 | 팀별 연락 | 팀 워크스페이스 토론 이력 관리 |
| 실무 관리 | 팀별 진행 상황 | 역할 범위에 맞는 팀 현황·마일스톤·보고서 상태 확인 |
| 실무 관리 | 진행 일정 관리 | 모집·수행·제출 기간과 팀 마일스톤 일정 관리 |
| 실무 관리 | 결과물 관리 backup(연도별) | 종료된 지난 프로젝트를 연도별로 검색하고 공개 결과물과 참여 이력 열람 |

영어 지원은 추천·심사 기능이 아니라 번역 기능만 의미한다. 외부 AI API는 사용하지 않고 로컬 Ollama만 호출한다.

## 3. 아키텍처

초기에는 배포가 단순한 모듈러 모놀리스로 구현한다. 각 기능은 다음 의존 방향을 지킨다.

```text
UI (Server/Client Components)
        ↓
Application Use Cases
        ↓
Domain Model / Policies
        ↓
Repository, OAuth, AI, Object Storage Adapters
```

### DDD와 SOLID 적용 범위

- `identity`, `project-program`, `topic`, `team`, `report`를 업무 경계로 나누고 각 모듈이 자기 규칙을 소유한다.
- 팀 정원·중복 소속, 보고서 상태 전이처럼 반드시 함께 일관성을 지켜야 하는 범위에만 Aggregate를 둔다.
- Domain 객체는 Prisma, Next.js, Ollama 같은 프레임워크 타입을 참조하지 않는 순수 TypeScript로 작성한다.
- Application 계층은 한 유스케이스의 순서를 조정하고, Domain 계층은 판단 규칙을 담당한다.
- 외부 변경 가능성이 있는 DB, 시간, 파일 저장소, 인증, 로컬 LLM 경계에만 인터페이스를 둔다.
- 하나의 구현만 존재하고 교체 가능성도 없는 내부 클래스에는 습관적으로 인터페이스를 만들지 않는다.
- React 컴포넌트, Server Action, Route Handler는 업무 규칙을 직접 구현하지 않는다.

### 오버 엔지니어링 방지 원칙

- 마이크로서비스, CQRS, Event Sourcing, 메시지 브로커, Kubernetes를 도입하지 않는다.
- 모든 엔티티에 Repository나 Service를 기계적으로 만들지 않는다.
- 공통 코드가 실제로 두 번 이상 나타나기 전에는 범용 `BaseRepository`, `BaseService`를 만들지 않는다.
- 도메인 이벤트는 같은 트랜잭션 안에서 반드시 필요한 후속 처리에만 사용하고, 초기에는 프로세스 내부 호출로 처리한다.
- 실시간 채팅, 벡터 DB, 별도 검색 엔진은 측정된 요구가 생기기 전까지 추가하지 않는다.
- 기능 하나를 `화면 → 유스케이스 → 도메인 규칙 → 저장소 → 테스트`의 세로 Slice로 완성한 뒤 다음 기능으로 이동한다.

권장 디렉터리 구조:

```text
src/
├── app/                         # 라우팅, 페이지, Route Handler
├── modules/
│   ├── identity/                # 사용자, 역할, 인증 후 프로비저닝
│   ├── academic-cycle/          # 학년도/학기, 프로젝트 운영 기간
│   ├── project-program/         # 관리자가 개설하는 동적 프로그램
│   ├── topic/                   # 주제 공개, 지원 조건, 탐색
│   ├── team/                    # 모집, 지원, 팀 확정, 팀원
│   ├── project/                 # 일정, 진행률, 활동 기록
│   ├── report/                  # 보고서 버전, 제출, 승인/반려
│   ├── communication/           # 팀/지도교수 스레드와 댓글
│   ├── artifact/                # 발표 영상과 결과물 메타데이터
│   ├── archive/                 # 완료 프로젝트 탐색
│   ├── translation/             # 로컬 LLM 기반 한국어/영어 번역
│   └── audit/                   # 중요 행위 감사 로그
├── shared/                      # 공통 타입, 오류, 인프라 어댑터
└── tests/                       # 통합 테스트 지원 코드
prisma/
├── schema.prisma
├── migrations/
└── seed.ts
docs/                            # 보고서, 발표자료, 설계 산출물
```

### 일관성 원칙

- 페이지는 조회 모델을 읽고 Use Case를 호출할 뿐, 업무 규칙을 소유하지 않는다.
- 모든 변경 API는 세션 확인, 역할 검사, 리소스 소유권 검사, 입력 검증을 수행한다.
- 팀 확정, 보고서 승인, 역할 변경처럼 중요한 변경은 DB 트랜잭션과 감사 로그를 함께 기록한다.
- 교수 대시보드의 진행률은 별도 수기 값이 아니라 마일스톤, 제출물, 승인 상태에서 계산한다.
- 파일 본문은 DB에 저장하지 않고 Object Storage에 보관하며 DB에는 키, 해시, 크기, MIME 타입, 버전을 저장한다.

## 4. 핵심 도메인 모델

### 계정과 운영 기간

- `User`: Google `sub`, 이메일, 이름, 역할, 활성 상태
- `ProfessorAllowlist`: 교수 권한을 받을 수 있는 이메일과 등록자
- `AcademicCycle`: 주제를 분류하는 학년도와 학기
- `ProjectProgram`: 관리자가 정하는 이름, 분류, 설명, 운영 기간, 공개 상태
- `AuditLog`: 행위자, 대상, 행위, 변경 전후 요약, 시각

프로그램 목록은 특정 대회명이나 개수를 코드에 고정하지 않는다. 관리자가 초안을 만들고 공개한 프로그램에만 교수가 주제를 등록할 수 있으며, 프로그램 마감은 미완료 주제와 대기 중인 지원·모집을 함께 종료한다.

### 주제와 팀 구성

- `Topic`: 작성자, 제목, 설명, 필요 기술, 모집 인원, 모집/수행/제출 기간, 상태, 공개 시각
- `TopicRequirement`: 지원 조건과 필수/우대 여부
- `StudentProfile`: 관심 분야, 보유 기술, 희망 역할, 가능 시간, 자기소개
- `TopicApplication`: 학생의 주제 지원, 지원 메시지, 처리 상태
- `RecruitmentPost`: 학생 주도 팀원 모집 글
- `RecruitmentApplication`: 모집 글 지원과 처리 상태
- `Team`: 학기, 주제, 지도교수, 팀명, 확정 상태
- `TeamMember`: 팀, 학생, 역할, 가입 상태

한 학생은 동일 학기의 확정 팀에 하나만 소속될 수 있도록 DB 제약과 도메인 정책을 모두 둔다. 팀 정원, 모집 기간, 중복 지원 처리도 서버 정책으로 보장한다.

주제의 모집·수행·제출 기간은 작성자가 각각 설정하며 서로 겹칠 수 있다. 각 기간 내부에서는 시작 시각이 종료 시각보다 앞서야 한다.

### 프로젝트 수행

- `Milestone`: 팀별 목표, 기한, 담당자, 상태
- `ProgressUpdate`: 진행 내용, 위험 요소, 다음 행동, 작성자
- `ScheduleItem`: 회의, 발표, 제출 기한
- `ConversationThread`: 팀 내부 또는 팀-지도교수 소통 채널
- `Comment`: 메시지, 첨부, 작성자, 수정 이력
- `Notification`: 지원 결과, 마감 임박, 보고서 승인 등 사용자 알림

실시간 채팅은 최소 요구사항에 포함하지 않는다. 업무 맥락과 이력이 남는 스레드형 댓글을 먼저 구현하고, 사용성 검증 후 WebSocket 알림을 추가한다.

### 보고서와 결과물

- `Report`: 착수/중간/결과 보고서 종류, 팀, 현재 상태
- `ReportVersion`: 버전 번호, 파일, 제출자, 제출 시각, 설명
- `ApprovalDecision`: 교수의 승인/수정 요청/반려, 의견, 처리 시각
- `Artifact`: 발표 영상, 소스 코드, 포스터 등 결과물의 파일 또는 외부 링크
- `ArchivedProject`: 공개 가능한 완료 프로젝트 메타데이터, 요약, 기술, 연도

보고서는 덮어쓰지 않고 새 버전으로 제출한다. 교수의 승인 대상은 특정 버전이며, 승인 후 새 파일이 등록되면 다시 검토 상태가 된다.

## 5. 권한 정책

| 행위 | 학생 | 교수 | 관리자 |
|---|---:|---:|---:|
| 공개 프로그램·주제/완료 프로젝트 조회 | O | O | O |
| 프로젝트 프로그램 개설·마감 | X | X | O |
| 학생 프로필 및 지원서 작성 | 본인 | 조회 제한 | 관리 |
| 주제 생성·공개 | X | 본인 주제 | 전체 |
| 팀 모집·지원 | O | 조회 | 관리 |
| 팀 확정 | 요청/동의 | 지도 대상 승인 | 전체 |
| 팀 일정·진행 기록 | 소속 팀 | 지도 팀 조회/피드백 | 전체 |
| 보고서 제출 | 소속 팀 | 조회 | 전체 |
| 보고서 승인 | X | 지도 팀 | 전체 |
| 교수 역할 부여 | X | X | O |
| 연도별 지난 프로젝트 아카이브 | 조회 | 조회 | 조회 |

목록 화면에서 버튼을 숨기는 것과 별개로 모든 서버 Use Case에서 권한을 다시 검사한다.

## 6. AI 기능 설계

AI는 한국어/영어 번역에만 사용한다. 주제·팀원 추천, 지원 자격, 팀 확정, 교수 승인에는 AI를 사용하지 않는다.

### 번역

- 주제 설명, 지원 메시지, 팀 토론, 선배 과제 요약을 한국어/영어로 번역한다.
- 원문은 변경하거나 덮어쓰지 않고 화면에서 요청한 번역문만 별도로 표시한다.
- 번역 결과는 외부 결정이나 상태 전이에 사용하지 않는다.

### AI 안전장치

- 구조화 출력 스키마, 시간 제한, 재시도 상한, 입력 길이 상한을 둔다.
- 인증된 사용자만 번역을 요청할 수 있고 원문 길이와 전역 동시 실행 수를 제한한다.
- 번역 장애 시 원문 조회와 모든 프로젝트 관리 기능은 정상 동작해야 한다.
- `LocalLlm` 포트를 통해 Ollama HTTP 호출을 격리하고 테스트에서는 가짜 구현을 사용한다.

### 로컬 LLM 실행 기준

- 기본 모델은 `qwen3.5:2b`로 고정한다. Ollama 배포본 기준 약 2.7GB이며, 한국어/영어 번역에 사용할 최소 실용 크기로 선택한다.
- `qwen3.5:0.8b`가 더 작지만 번역 품질 저하 가능성이 커서 기본 모델로 사용하지 않는다.
- 16GB 통합 메모리에서 앱, Docker, 브라우저와 함께 실행해야 하므로 Context는 8K, 병렬 요청은 1개로 제한한다.
- 품질 검증을 통과하지 못한 경우에만 `qwen3.5:4b`로 올린다. 처음부터 여러 모델이나 별도 추론 서버를 운영하지 않는다.
- Ollama는 `OLLAMA_NO_CLOUD=1`로 실행해 Cloud 모델과 웹 검색을 비활성화한다.
- macOS Docker Desktop은 GPU passthrough를 제공하지 않으므로 Ollama만 macOS 네이티브로 실행해 Metal을 사용한다.
- PostgreSQL과 MinIO는 Docker Compose로 실행한다. Next.js 개발 서버는 빠른 Hot Reload를 위해 호스트에서 실행하고, CI·운영 이미지는 Docker로 검증한다.
- Ollama의 JSON 출력을 사용하고 같은 Zod 스키마로 서버에서 다시 검증한다.

## 7. 주요 화면

### 기본 사용자 흐름

```text
로그인 → 프로젝트 탐색 ─ 진행 중(프로그램 태그·주제) → 지원/팀원 모집 → 내 프로젝트
                    └ 종료됨(연도·프로그램·결과물 참고)
                                      팀 확정 후 ↘ 일정·진행·보고서·결과물
```

- 모든 역할의 로그인 완료 첫 화면과 앱 로고의 홈은 `/topics`로 통일한다.
- 학생의 첫 질문은 "어떤 프로젝트가 열렸는가"이며, `내 프로젝트`는 지원 또는 팀 확정 이후 사용하는 개인 작업공간이다.
- 진행 중 주제와 지난 프로젝트는 `/topics`의 한 프로젝트 탐색 화면에서 전환한다. 별도 전역 메뉴나 별도 제품 영역으로 나누지 않는다.
- 전역 메뉴는 프로젝트 탐색, 역할별 프로젝트, 역할별 핵심 작업의 3개 축으로 제한한다.
- 교수·관리자의 상세 운영 항목은 전역 메뉴에 늘어놓지 않고 관리 화면 안의 컨텍스트 메뉴로 단계적으로 공개한다.
- 핵심 기능은 로그인 후 최대 3번의 이동 안에 도달하고, 빈 상태에는 주제 탐색이나 주제 등록으로 이어지는 복구 행동을 제공한다.

### 공통

- 랜딩 및 Google 로그인
- 최초 로그인 프로필 설정
- 알림함
- 진행 중·종료된 프로젝트 통합 탐색
- 프로그램별 공개 주제와 완료 프로젝트 검색/상세

### 학생

- 공개 주제 탐색, 필터, 상세, 지원
- 주제 설명과 지원 메시지 번역
- 팀원 모집 글 목록/작성/지원
- 내 팀 워크스페이스
- 일정·마일스톤·진행 기록
- 지도교수 소통 스레드
- 보고서 버전 제출과 승인 상태
- 결과물 및 발표 영상 등록

### 교수

- 주제 작성, 지원 조건 설정, 공개/마감
- 지원자 및 팀 선택/확정
- 지도 팀 통합 대시보드
- 팀별 일정, 진행률, 위험 신호 확인
- 팀 소통 및 피드백
- 착수/중간/결과 보고서 웹 승인 또는 수정 요청
- 결과물과 연도별 아카이브 조회

### 관리자

- 학년도/학기 관리
- 프로젝트 프로그램 생성·공개·마감
- 교수 이메일 허용 목록과 역할 관리
- 사용자/팀/콘텐츠 상태 관리
- 완료 프로젝트와 공개 결과물의 연도별 조회

## 8. API 및 Use Case 경계

외부 연동이나 파일 업로드처럼 명시적 HTTP 계약이 필요한 기능은 Route Handler를 사용한다. 같은 Next.js 화면에서 발생하는 단순 변경은 Server Action을 사용할 수 있지만 둘 다 동일한 Application Use Case를 호출한다.

```text
/api/auth/*                       Google OAuth callback/session
/api/uploads/presign              권한 검사 후 업로드 URL 발급
/api/uploads/complete             파일 해시·메타데이터 확정
/api/translations                 로컬 LLM 한국어/영어 번역
/api/cron/deadlines               마감 알림 생성
```

대표 Use Case:

- `PublishTopic`, `ApplyToTopic`, `CloseTopic`
- `CreateRecruitmentPost`, `ApplyToRecruitment`, `ConfirmTeam`
- `RecordProgress`, `CompleteMilestone`, `SendTeamMessage`
- `SubmitReportVersion`, `ApproveReportVersion`, `RequestReportRevision`
- `RegisterArtifact`, `ArchiveCompletedProject`
- `CreateProjectProgram`, `OpenProjectProgram`, `CloseProjectProgram`, `TranslateContent`

## 9. 검색, 파일, 연도별 아카이브

### 검색

- 첫 버전은 PostgreSQL Full Text Search와 태그 필터를 사용한다.
- 주제, 기술, 연도, 지도교수, 요약을 검색 대상으로 둔다.
- 데이터 규모와 실제 검색 품질을 측정하기 전에는 별도 검색 엔진이나 벡터 DB를 도입하지 않는다.

### 파일 업로드

- 브라우저는 서버가 발급한 제한 시간 Presigned URL로 Object Storage에 직접 업로드한다.
- 서버가 허용 확장자, MIME 타입, 크기, 소유권을 검사한다.
- 업로드 완료 후 해시와 Object Key를 DB에 확정하고 미완료 업로드는 정리한다.
- 영상은 기본적으로 파일 업로드와 외부 영상 링크를 모두 지원하되 최대 크기는 운영 환경에서 설정한다.

### 연도별 아카이브

- 최종 보고서 승인 후 종료된 프로젝트는 수행 연도 기준으로 계속 열람할 수 있어야 한다.
- 연도, 주제, 팀, 지도교수, 기술을 검색 조건으로 제공하고 공개 결과물 링크를 함께 보여 준다.
- 여기서 `backup(연도별)`은 파일시스템이나 DB 복제 기능이 아니라 지난 프로젝트를 보존하고 찾아보는 제품 기능을 뜻한다.

## 10. 구현 단계

### 단계 0 — 프로젝트 기반과 설계 고정

- Next.js/TypeScript 프로젝트와 DDD 모듈 경계 구성
- Docker Compose로 PostgreSQL과 MinIO 개발 환경 구성
- macOS 네이티브 Ollama와 `qwen3.5:2b` 설치·상태 확인 절차 구성
- Prisma 스키마와 마이그레이션 규칙 작성
- 환경변수 검증, 오류 형식, 로깅, 테스트 기반 구성
- ERD, 화면 흐름, 권한표를 `docs/`에 확정

완료 조건: 새 환경에서 Docker Compose로 PostgreSQL과 MinIO를 실행하고, 앱이 Ollama 상태를 진단하며, CI에서 lint, typecheck, unit test가 통과한다.

### 단계 1 — 인증과 역할

- Google OAuth와 `pusan.ac.kr` Workspace 검증
- 사용자 최초 프로비저닝과 학생 프로필
- 관리자/교수 허용 목록, 역할 변경 감사 로그
- 서버 중심 RBAC/소유권 정책 테스트

완료 조건: 개인 Google 계정과 다른 도메인은 거절되고, 학생·교수·관리자별 허용/거부 시나리오가 통합 테스트로 검증된다.

### 단계 2 — 학기, 프로그램, 주제, 완료 프로젝트

- 학년도/학기 관리와 관리자의 동적 프로그램 개설·공개·마감
- 열린 프로그램 안에서 주제 작성자의 모집/수행/제출 기간 설정
- 교수 주제 CRUD, 공개/마감, 지원 조건
- 학생 주제 검색·지원
- 완료 프로젝트 아카이브 검색

완료 조건: 교수의 주제 공개부터 학생 지원까지 핵심 흐름이 E2E로 동작한다.

### 단계 3 — 팀 모집과 확정

- 학생 프로필의 기술/역할/가능 시간
- 모집 글, 지원, 수락/거절
- 주제 기반 팀 선택과 교수 승인
- 동일 학기 중복 팀 가입 및 정원 경쟁 상태 방지

완료 조건: 동시 수락 상황에서도 정원과 단일 팀 제약이 깨지지 않는다.

### 단계 4 — 팀 워크스페이스와 교수 대시보드

- 마일스톤, 일정, 진행 기록
- 팀 내부/지도교수 스레드
- 파생 진행률과 위험 신호
- 마감 알림

완료 조건: 학생 기록이 교수 대시보드에 일관되게 반영되고 권한 없는 팀 데이터는 노출되지 않는다.

### 단계 5 — 보고서 승인과 결과물

- Presigned URL 파일 업로드
- 착수/중간/결과 보고서 버전 제출
- 교수 웹 승인, 수정 요청, 재제출
- 발표 영상과 결과물 등록
- 완료 프로젝트 아카이브 전환

완료 조건: 제출 버전별 승인 이력과 파일 무결성을 추적할 수 있다.

### 단계 6 — 로컬 LLM 번역

- Ollama `LocalLlm` 어댑터와 Zod 구조화 출력
- 주제 설명, 지원 메시지, 팀 토론, 아카이브 요약의 한국어/영어 번역
- 인증, 입력 길이, 동시 실행 제한
- 장애·시간 초과·잘못된 모델 출력 처리

완료 조건: 로컬 LLM을 끄거나 장애를 발생시켜도 원문 조회와 프로젝트 업무 흐름은 정상 동작한다.

### 단계 7 — 운영 안정화

- 연도별 지난 프로젝트 탐색과 공개 범위 점검
- 보안 헤더, Rate Limit, 업로드 제한, 감사 로그 점검
- 접근성, 모바일 반응형, 성능 측정
- 프로덕션 Docker 배포와 운영 문서

완료 조건: 지난 프로젝트의 연도별 검색·결과물 열람과 핵심 E2E, 보안·권한 테스트, 배포 Smoke Test가 통과한다.

## 11. 테스트 전략

- 도메인 단위 테스트: 지원 기간, 팀 정원, 중복 소속, 보고서 상태 전이
- Use Case 통합 테스트: 실제 PostgreSQL을 사용한 트랜잭션과 권한 검사
- 계약 테스트: Google 프로필 매핑, 번역 구조화 응답, Object Storage 어댑터
- 컴포넌트 테스트: 폼 검증, 역할별 UI, 접근성
- E2E 테스트: 로그인 이후 학생 지원, 팀 확정, 보고서 제출/승인의 전체 흐름
- 운영 테스트: 대용량 업로드 실패 복구, AI 시간 초과, 연도별 아카이브 조회, 중복 요청

각 기능은 정상 경로뿐 아니라 권한 없음, 마감 경과, 중복 요청, 동시성 충돌을 함께 검증해야 완료로 본다.

## 12. 초기 범위에서 제외할 기능

- 모바일 네이티브 앱
- 화상 회의 및 실시간 채팅
- AI 주제 추천, 팀원 추천, 자동 팀 확정, 자동 보고서 승인
- 자체 동영상 스트리밍/인코딩 플랫폼
- 데이터 규모 검증 전 벡터 DB, 마이크로서비스, Kubernetes 도입
- 외부 AI API와 Cloud 모델 호출

## 13. 구현 전 필요한 외부 설정

- Google Cloud OAuth Web Client와 로컬/운영 Redirect URI
- `pusan.ac.kr` 계정으로 실제 `hd` 클레임이 반환되는지 사전 검증
- 최초 관리자 이메일과 교수 이메일 허용 목록
- Docker Desktop
- macOS 네이티브 Ollama와 `qwen3.5:2b` 모델
- 운영 환경의 PostgreSQL 접속 정보
- 운영 환경의 S3 호환 Object Storage 버킷과 접근 키
- 운영 도메인, 메일/알림 발송 방식

외부 AI API 키는 필요하지 않다. 애플리케이션은 `OLLAMA_BASE_URL`과 `OLLAMA_MODEL=qwen3.5:2b`만 설정하며, 로컬 LLM 장애가 핵심 업무 기능을 막지 않도록 한다.
