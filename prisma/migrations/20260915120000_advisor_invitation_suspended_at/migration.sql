-- 심사를 잠시 멈추는 자리.
--
-- 링크가 새어 나간 것이 아니라 잠시 손을 떼게 해야 할 때, 초대를 거두면 담당 팀 배정까지
-- 지워지고 토큰을 거두면 풀 때 새 링크를 다시 전달해야 한다. 링크 원문은 저장하지 않으므로
-- 운영자가 예전 링크를 다시 보여 줄 수도 없다.
--
-- 이 칸이 차 있으면 토큰이 살아 있어도 접속을 막는다. 비우면 위원이 가진 그 링크가 그대로
-- 열린다. 초대와 담당 팀 배정은 처음부터 끝까지 손대지 않는다.
ALTER TABLE "program_advisor_invitation" ADD COLUMN "suspendedAt" TIMESTAMP(3);
